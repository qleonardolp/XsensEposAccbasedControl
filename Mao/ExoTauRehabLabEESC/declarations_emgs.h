#ifndef DECLARATIONS_EMGS
#define DECLARATIONS_EMGS

/*
#pragma comment(lib, "Ws2_32.lib")
#include <WinSock2.h>

SOCKET commSock = INVALID_SOCKET ;
SOCKET emgSock;
SOCKET accSock;

struct structEMG_vars {
  bool Connect;
  double * dataEMG;
  int T;
  char response[32];
	char tmp[128];
  char tmp2[100000];
  int k;	//byte count
	char emgbuf[4*16];		//buffer for one sample from each sensor
	char accbuf[4*3*16];	//buffer for one sample on each axis for each sensor
	char imemgbuf[4*16];   //buffer for one sample from each IM sensor
	unsigned long bytesRead;	//bytes available for reading
	float emgsample;	//EMG sample
  double currentValues[16];
};
*/

/*
class EMG {


};
*/


#endif /* DECLARATIONS_EMGs */